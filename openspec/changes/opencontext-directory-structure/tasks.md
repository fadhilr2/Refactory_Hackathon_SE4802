## 1. Create Root Directories

- [x] 1.1 Create `cli/` directory in the repository root and add `.gitkeep`
- [x] 1.2 Create `pipeline/` directory in the repository root and add `.gitkeep`
- [x] 1.3 Create `inference/` directory in the repository root and add `.gitkeep`

## 2. Verification

- [x] 2.1 Verify that the root directory contains the created directories (`cli/`, `pipeline/`, and `inference/`)
